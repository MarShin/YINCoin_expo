import ESylesheet from 'react-native-extended-stylesheet';

export default ESylesheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '$white',
  },
});
